
### 仮説

- データをロジックをまとめられるならジェネリクスが使えるが、異なるデータを運ぶだけの時は結局Type Switchが発生するし、ジェネリクスを使うメリットが薄い。
- 受け取り側がデータの種類によって処理を分ける必要があるということは、データに依存がある。そもそもデータに依存があるなら、カプセル化されていないのでその部分の設計の見直しが必要になる。
- ドメインのあるエンティティにバリエーションがある場合は、そのエンティティを使う側でType Switchを使う必要がある。
- もしも依存を解決したモジュールを使うならば種類ごとの分岐はいらないが、種類ごとに処理をする関数か何かを用意する必要がある。
